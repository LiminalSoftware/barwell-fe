import { createContainer } from "items-store";
import ModelPane from "containers/ModelPane";
import DetailBar from "containers/DetailBar";

export default createContainer(ModelPane);
// export default createContainer(DetailBar);